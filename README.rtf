Ftest contient des sections avec les tests correspondant aux differentes fonctions utilisées pour l'algorithme Ford-Fulkerson.
Il suffit de dé-commenter la partie correspondante à la fonction que vous voulez tester.
Le fichier Makefile est déjà configuré pour tester l’algorithme sur le graph1.txt avec 
comme source 0 et puit 5 et mettre le résultat sur rfinal, et ensuite le convertir en format .svg
à partir du rfinal.dot sur le fichier « some-output-file.svg ». Pour compiler le programme, 
il suffit d’écrire « make » sur le terminal et ensuite « make demo » pour exécuter le code. 
Il suffit de remplacer sur le makefile la source et le puit ainsi que le graph sur lequel faire les tests. 


